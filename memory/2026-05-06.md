# 2026-05-05 / 2026-05-06 — Session Notes

## PWA Features Built (May 5, 2026)

### 🔄 Swipe Gestures on Prospect Cards
- **Swipe right** → green "📅 BOOK" overlay → opens Google Calendar with pre-filled prospect details
- **Swipe left** → red "♻️ SKIP" overlay → card animates off-screen, removed from list
- Touch (mobile) + mouse drag (desktop), 80px threshold
- Added to `ProspectSearch.svelte` prospect result cards (not saved cards)

### 🔙 Global Edge-Swipe-Back Navigation
- Swipe from left edge (~30px) → triggers `goBack()` in active component
- Implemented at **Main.svelte** level via `CustomEvent('edge-swipe-back')`
- Wired into: ProspectSearch, Clients, Tools, Present, StoreDetail
- Works across all tabs, respects each component's navigation stack

### 📊 Zone Filter Reactivity Fix
- **Bug:** Analytics stats (yearly/monthly/rep) didn't update when switching zones — Svelte wasn't detecting dependency
- **Fix:** Made `filteredContracts`, `yearlyStats`, `monthlyStats`, `repStats` reactive (`$:` declarations) instead of imperative function calls
- Renamed functions to `calcYearlyStats(filtered)` etc. with explicit param

### 🏠 Home Screen Redesign
- **New layout order:** Quote → Greeting (time-of-day) → Quick Actions → Today at a Glance → Revenue → Stats → Daily Goal
- Quick Actions moved to top: Find Prospects (red primary), Stores, Book Appt, Drive Mode
- Cycle info condensed into pill badge under greeting
- Appointment card made full-width (removed mini goal sidebar)
- Quote stays at top per Tyler's preference

### 🔧 Sort Bar Responsive Fix
- Sort buttons + list/map toggle now use `flex-wrap` so they render properly on large text sizes

### 🚗 Driving Mode
- **New component:** `DrivingMode.svelte` — full-screen dark UI for driving
- Auto-briefing via Speech Synthesis API (reads appointments, goal, revenue)
- Navigate button → Apple Maps directions
- Call button → dials prospect phone from appointment details
- Log Call → increments daily goal counter
- Wake Lock keeps screen on
- URL deep links: `?mode=driving` and `?action=logcall` (for Siri Shortcuts)
- Shortcuts setup page at `/shortcuts.html`
- Dark button in Quick Actions grid

### 📱 Siri Shortcuts / CarPlay
- Created `/shortcuts.html` with setup instructions
- Three shortcuts: "Sales briefing" (→ driving mode), "Next appointment", "Log a call"
- Work through CarPlay via Siri voice commands
- Uses URL scheme deep links into the PWA

## Deployment Notes
- **gh-pages branch** is what GitHub Pages deploys from (not `main` directly)
- Must run `npx gh-pages -d dist` after `npm run build` to push built files
- PWA service worker can aggressively cache — users may need hard refresh
- Claude Code CLI was not authenticated (`/login` needed) — had to implement features directly

## Contracts
- **Morning scan (9:47 AM):** 133 contracts synced
- **Evening scan (8:00 PM):** 1 new — **La Cocina Oaxaca** (Megan Wink, $3,542)
- Total in system: 401 line items, 134 unique contracts

## Team Notes
- Megan Wink closing deals again — La Cocina Oaxaca $3,542
- Amy Dixon strong — Papa Murphy's $15,075 (3 stores)
