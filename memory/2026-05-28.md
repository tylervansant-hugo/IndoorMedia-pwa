# Memory — May 25–28, 2026

## Contracts Scan Fix (May 25)
- **Bug:** `rebuild_contracts.py` re-stamped `extracted_at` on ALL contracts every run → nightly report always showed all 415 as "new"
- **Fix:** Now preserves existing `extracted_at` timestamps; only truly new contracts get today's timestamp
- Reset all 415 contracts to baseline `2026-05-01T00:00:00` since none were actually new that night
- Commit: `fix: preserve extracted_at timestamps to stop re-reporting old contracts`

## Rate Card Update (May 26)
- Tyler sent new CSV with ~1,700+ stores across all zones
- Compared against 7,835 existing stores in `stores.json`
- **Results:** 2,159 stores updated, 65 new stores added → 7,900 total
- Key changes: zone formatting (bare digits → zero-padded), PostalCode leading zeros, rate changes (~150), cycle rotations (~50)
- **Must run `npm run build && npx gh-pages -d dist`** to deploy — pushing to `main` alone does NOT update the live site (gh-pages branch serves the built app)

## ROI Calculator Rewrite (May 26)
- Tyler's new compounding monthly formula across ALL 4 files:
  - `roi.js`, `ROICalculator.svelte`, `StoreSearch.svelte`, `Tools.svelte` (+ PDF export)
- **Formula:** Gross Revenue = NewCustomers × AvgSpend × 78 (where 78 = 12×13/2, compounding monthly)
- **Verification:** 25 × $50 × 78 = $97,500
- Removed all coupon/redemption/discount/scenario logic — single clean calculation
- Steps: Gross Revenue → COGS ($) → Net Revenue → Net Profit → ROI %

## Cameron Gould Onboarding (May 26)
- **Rep ID:** 2126715079 | **Zone:** 24X | **Location:** Utah
- Tyler logged into Cameron's Gmail on the Mac mini with Cameron's permission
- Auth'd `cameron.gould@indoormedia.com` for Gmail + Drive via `gog auth add`
- **Extracted:** 93 contracts ($398,812.78 total revenue) from signed contract PDFs
- **Extracted:** 58 ad proofs with image URLs (from `GetGoogleDriveCoupon?id=` pattern in zone emails)
- **Data format lessons:**
  - Ad proofs need `reps: [{email, name}]` array + `recipients: [email]` — not just a `rep` string field
  - Contracts must go in `contracts.json` (not just `prospect_data.json`) for the Clients tab to show them
  - `prospect_data.json` is a dict keyed by contract number (not a list)
- **Zone install day fix:** Added `zone: "24X"` to Cameron's rep_registry entry + updated `getZoneInstallDay()` in Main.svelte to check `$user.zone` field first (Zone 24 installs on the 14th, not default 7th)
- Login.svelte updated to pass `zone` + `assigned_stores` to user object

## Store Dibs Feature (May 27)
- Reps can "claim" a store → visible to all reps → expires Saturday midnight
- **Firebase issue:** `store_claims` collection had no Firestore write permissions
- **Fix:** Moved claims to `activity_daily` collection (already has write perms) with `dibs_` prefix on doc ID and `type: 'store_claim'`
- Added to: StoreSearch.svelte (Rates tab) + ProspectSearch.svelte (Prospects tab)
- Immediate local state update after claiming (don't wait for Firebase re-fetch)
- Uses store's zone (not user's zone) when saving claims

## Shared Store Context (May 28)
- Created shared Svelte stores: `sharedNearbyStores`, `sharedSelectedStore`, `sharedUserLocation`, `sharedStoreSearch`
- When a rep searches stores or does Near Me in Rates → same stores appear in Prospects + Map auto-centers
- Works bidirectionally (Prospects → Rates/Map too)
- Prevents re-searching when switching between Rates/Prospects/Map sub-tabs

## Deployment Reminder
- **ALWAYS** run `npm run build && npx gh-pages -d dist` after pushing data or code changes
- Pushing to `main` updates the repo but NOT the live site
- Live site: https://tylervansant-hugo.github.io/IndoorMedia-pwa/
