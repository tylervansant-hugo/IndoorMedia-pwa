# 2026-04-02 — Daily Log

## 2026 Renewals Data Loaded into PWA (Major)
- **Manually transcribed all 6 cycles** from PDF: B2 (45), C1 (36), B1 (51), A2 (48), C2 (52), A1 (41)
- PDF parsing kept failing/timing out — manual extraction was faster
- Created `scripts/parse_2026_renewals.py` to combine all cycles
- **259 clean records** (was 269, removed 10 duplicates — same contract with run=4 and run=1 entries, kept the one with revenue)
- Data at `pwa/public/data/pending_renewals.json`
- Zones: 07W (26), 07X (90), 07Y (76), 07Z (77)
- Total contract value: $2,915,552.91
- Top reps: Kale Lundberg (37), Richard Diamond (35), Dave Boring (25), Tyler VanSant (24)

## Renewals UI (Clients.svelte)
- Fixed duplicate `{#if view === 'renewals'}` blocks — removed first, kept second
- Added `fmtPrice()` helper for number/string price formatting
- Fixed field names: `address`, `city`, `state`, `zip` (not `storeAddress`/`storeCityStateZip`)
- **Filters added:** Zone (07W/X/Y/Z with counts), Rep, Cycle, Search
- Commits: 82fe6a8, 838e9e5, f61dd02

## Hot Leads Fixes (ProspectSearch.svelte)
- **Added Rep filter** — maps store_id → rep name from rep_registry.json
- **Fixed dark mode** — card backgrounds, business names, categories, store text all use CSS variables now
- Hot leads data has NO rep field — rep assignment derived from store assignments in registry
- Commit: 84cd4a0

## Appointments & Calendar Sync
- **Hourly calendar sync cron** — runs every hour 7AM–9PM Pacific via `scripts/sync_calendar_hourly.sh`
  - Replaced old 2x/day crons (2PM + 10PM)
  - Uses `scripts/sync_calendar.py` → `gog` CLI → appointments.json → gh-pages deploy
- **Sync Now button** on Appointments dashboard card — force-refreshes from server
- **Book Appointment button** in Quick Actions + Appointments section
  - Opens Google Calendar with pre-filled event
  - **Tyler auto-invited** to every appointment (`tyler.vansant@indoormedia.com` hardcoded in calendar URL)
  - Also updated ProspectSearch "Book Appointment" link to auto-invite Tyler
- Commit: 0984a74

## Dashboard UI Improvements
- **Larger stat cards:** min-height 200px (mobile), 180px (desktop)
- **Bigger icons:** 42px on mobile (was 32px)
- **Bigger stat values:** 36px on mobile (was 28px)
- Cards have hover lift + active press effects
- Appointment drill-down: bigger padding, 18px title, styled date badges
- Commit: 8d226c6

## Per-Rep Daily Goals
- Goals now save with per-rep keys (`impro_daily_goal_{userId}`)
- Backward compatible — migrates old `impro_daily_goal` data
- Created `public/data/rep_sync.json` + `scripts/sync_rep_data.py` for shared data foundation
- Full real-time cross-device sync would need Firebase/Supabase (not yet implemented)

## Browser Caching Issues
- Tyler's phone kept loading old cached versions despite deployments
- Cache buster query params (`?v=XX`) help but iOS Safari is aggressive
- `--no-history` flag on gh-pages deploy helps but doesn't fully solve
- May need to add version number to data filenames or service worker cache busting

## Deployment Summary
- Latest commit: 8d226c6
- Deploy URL: https://tylervansant-hugo.github.io/IndoorMedia-pwa/
- All changes built + deployed via `npm run build && npx gh-pages -d dist --no-history`

## Tyler's Requests (in order today)
1. ✅ Load all 2026 renewal data
2. ✅ Add zone filter to renewals
3. ✅ Remove duplicate renewals
4. ✅ Fix dark mode on hot lead cards + add rep filter
5. ✅ Cross-device sync for appointments/goals + manager auto-invite
6. ✅ Hourly calendar sync + sync button
7. ✅ Larger dashboard cards + calendar display improvements
