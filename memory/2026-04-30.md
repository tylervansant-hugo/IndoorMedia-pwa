# 2026-04-30 — Daily Notes

## PWA v2 Simplification Discussion (Apr 29)
- Tyler asked how to simplify the PWA for adoption
- Proposed cutting from 7 tabs → 3 (Home, Prospect, Tools)
- Key insight: reps open it, see 7 tabs, don't know where to start, close it
- Goal: < 3 taps to do anything from the parking lot
- Built HTML mockup at `mockups/pwa-v2.html` — Tyler liked the direction
- **Not implemented yet** — mockup only, Tyler may revisit

## PWA Changes Made Today

### Testimonial Clickable Links (Built then Reverted)
- Made testimonial cards clickable (open on IndoorMedia site) + "Open All" button
- Also fixed consistent font sizes on prospect grid buttons (15px title, 13px desc)
- **Tyler reverted all changes** back to 8pm Apr 28 state — wanted original look/feel preserved
- Revert commit: `97027e1`

### Bottom Padding / Scroll Fix (Apr 29–30)
- Tyler reported content hidden behind nav bar on multiple pages
- Added 140px bottom padding to ALL pages/tabs:
  - Main.svelte (.content, .dashboard, .analytics-container)
  - ProspectSearch.svelte (.prospects-container)
  - Tools, StoreSearch, Clients, Products, Cart, Present, TestimonialSearch
  - AdminPanel, MyCustomers, PerformanceDashboard, ShippingStatus

### Font Size Consistency (Apr 30)
- "Nearby Advertisers" button used `<a>` tag vs `<button>` for others → different font rendering
- Fixed all `.btn-text` to 15px, `.btn-desc` to 13px, `.btn-icon` to 32px (explicit px, not rem)

### Text Message Templates (Apr 30) ✅ LIVE
- Added 6 SMS-specific templates to Prospect results (💬 Text button)
- Templates auto-fill: rep name, store (chain + street + city), business name, subcategory
- Each has Copy + Send (opens SMS pre-filled)
- Templates:
  1. 🤝 Cold Intro
  2. 📊 Value Drop
  3. ⏰ Follow-up
  4. 🔥 Last Spot
  5. 🔄 Re-engage
  6. 📸 Post-Visit
- Tyler specifically wanted SMS tone (short, casual) — first draft was too email-like
- Store name format changed from code ("SAF07X-0186") to human: "Safeway on Main St in Kingston"
- Function: `getTextTemplates(prospect)` in ProspectSearch.svelte

## Contracts Scan Issues (Apr 29)
- Nightly scan reported 136 "new" contracts but only 1 was actually new (Better Vape, $6,575)
- Dedup logic needs fixing — re-flagging old contracts as new
- Tyler flagged this — should investigate `run_contracts_scan.sh` dedup

## GitHub Pages Deploy Gotcha
- Using `npx gh-pages -d dist --no-history` caused asset 404s (index.html updated but JS/CSS missing)
- Fix: use `npx gh-pages -d dist` (without --no-history) for clean deploys
- GitHub Pages CDN can take 15-30 seconds to propagate after deploy
