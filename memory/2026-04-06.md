# Session: Monday, April 6, 2026

## Counter Sign Generator API Restart
- **Issue:** Counter Sign generator failing with "Error: Load failed" — Cloudflare tunnel had expired
- **Fix:** Restarted `start_counter_sign_service.sh`, which creates fresh tunnel URL
- **New tunnel:** `https://hansen-welding-finance-drug.trycloudflare.com` (persists only while service running)
- **Note:** Tunnel URLs are ephemeral; each restart generates a new one. Consider setting up persistent tunnel if needed for long-term stability
- **Status:** ✅ Live and working

## Renewal Auto-Matching System (COMPLETE)
**Problem:** Manual matching of renewals to new contracts is tedious
**Solution:** Built smart contract matching logic
- **Match Strategy (in order):**
  1. Contract number exact match (strongest — `J407143E` = `J407143E`)
  2. Store number + fuzzy business name (extracts `4041` from `ALB07W-4041`)
  3. Fuzzy name matching (handles variations like "Banano Smoothies/Spinners" vs "Spinners Frozen Yogurt")
- **UI Changes:**
  - Green **✅ RENEWED** badge appears on renewal card header when matched
  - Expanded view shows "✅ RENEWED — Contract matched" banner
  - Matched renewals don't show urgency deadline (contract is already handled)
- **Features:**
  - Banano/Spinners test case: ✅ RENEWED (contract `J407143E` matched)
  - Auto-updates as new contracts sync into contracts.json
- **Files:** Updated matching logic in Clients.svelte `getRenewalStatus()`

## Renewal Follow-up Scheduling (COMPLETE)
Added to each expanded renewal card:
- **📞 Schedule Call** — Opens Google Calendar event for 30 min call, pre-fills with all renewal details
- **🚗 Schedule Visit** — Opens Google Calendar event for 1 hour visit, includes business address as location
- Both auto-invite Tyler (tyler.vansant@indoormedia.com)
- Default: tomorrow at 10 AM (or 10-11 AM for visits)

## Email Template Spacing Fix
- **Problem:** Renewal email templates had bullets collapsing into single lines on mobile mail apps
- **Solution:** 
  - Converted all templates to proper multi-line strings with blank lines between paragraphs
  - Replace `\n\n` with `\r\n\r\n` (CRLF) in mailto encoding
  - Changed bullet syntax from `•` to `-` (renders more reliably)
- **Affected Templates:** All 4 renewal templates (Reminder, Value, Urgency, Win-Back) + all prospect templates
- **Result:** Paragraphs now display with proper spacing on iOS/Android mail clients

## Testimonial Features (Prospects Tab)
**Improvements:**
1. **Clickable testimonial links** — "🔗 View Full Testimonial →" opens testimonials.indoormedia.com
2. **Nearby business testimonial** — Automatically appends one testimonial from same city or chain (marked with blue border + "📍 Nearby Business" badge)
3. **Clean HTML** — Decodes HTML entities (`&#x27;` → `'`, `&amp;` → `&`)
4. **Fallback loading** — Tries testimonials_slim.json first, falls back to testimonials_cache.json

## Nearby Advertisers Link (NEW FEATURE)
Added to 3 tabs:
1. **Stores** (Store Detail) — "📋 View Current Nearby Advertisers" button above pricing
2. **Prospects** — "📋 Nearby Advertisers" in action row (next to Maps, Mappoint)
3. **Clients** (Renewals) — "📋 View Current Nearby Advertisers" link at top of section
- **URL Format:** `https://coupons.indoormedia.com/?location={City},{State}` (location pre-filled based on selected store)
- **Use Case:** Reps can show prospects who's already advertising at that location

## Counter Sign Rep Name Dropdown (COMPLETE)
**Feature:** Auto-generate rep landing page URLs in counter sign generator
- **Step 2 (Landing Page):** Replaced URL input with dropdown
- **Rep List:** Loaded from rep_registry.json (sorted alphabetically)
- **Auto-fill URL:** `https://www.indoormedia.com/tape-sales/advertise-with-{first}-{last}/`
  - Example: "Jan Banks" → `https://www.indoormedia.com/tape-sales/advertise-with-jan-banks/`
- **Live Preview:** Shows the generated URL below dropdown so reps can verify
- **Fallback:** "Can't find your name?" link allows manual URL entry
- **Benefit:** Eliminates typos, makes process faster

## Latest Deployment
- **Live URL:** https://tylervansant-hugo.github.io/IndoorMedia-pwa/?v=134
- **Latest commit:** `080ee37` — Counter Sign rep name dropdown
- **Test cases passed:** Banano/Spinners renewal matching, email spacing on iOS, counter sign API working

## Nightly Contracts Sync (8:00 PM) & Smart Reporting Fix
**Problems Identified:**
- `extracted_at` was set to script run time (NOW) instead of actual contract date
- This made it impossible to identify which contracts were TRULY new
- First sync report was misleading (showed 305 "new" when previous scan was empty)

**Solution Implemented:**
1. **Updated `rebuild_contracts.py`** — now uses actual contract date from PDF as `extracted_at`, not the script timestamp
2. **Created `report_new_contracts.py`** — tracks previous scan state and identifies genuinely NEW contracts by comparing contract numbers
3. **Updated `run_contracts_scan.sh`** — now runs 3-step process:
   - Step 1: Scan Gmail + download PDFs
   - Step 2: Parse PDFs, rebuild contracts.json with actual dates
   - Step 3: Generate smart report of new contracts (by comparing to previous scan)
   - Step 4: Push to GitHub

**Key Files:**
- `/Users/tylervansant/.openclaw/workspace/data/contracts_previous.json` — saves state from previous scan
- `scripts/report_new_contracts.py` — smart diff engine
- `scripts/rebuild_contracts.py` — updated to use contract date

**La Real IS in contracts.json:** ✅ Megan Wink, store 0040, $5,450 Single Ad (extracted 2026-04-06)

## Open Questions/Next Steps
- Counter Sign tunnel URL refreshes each service restart — should we set up persistent tunnel?
- All core PWA features now complete and pitch-ready ✅
